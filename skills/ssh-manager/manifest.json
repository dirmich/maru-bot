{
  "name": "ssh-manager",
  "version": "1.0.0",
  "description": "Automated SSH connection and public key authentication management.",
  "always": true,
  "required_tools": ["shell"],
  "permissions": ["execute_command"]
}
